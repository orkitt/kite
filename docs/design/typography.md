# Typography

`KiteTypography` is a `ThemeExtension<KiteTypography>`.

```dart
Text(
  'Dashboard',
  style: context.typography.heading,
)
```

The package also exports `KiteTextStyleX` for common `TextStyle` transformations.
